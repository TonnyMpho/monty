#include <stdio.h>
#include "monty.h"

/**
 * nop - function thatdoes nothing
 * @stack: pointer to the stack
 * @line_number: current line
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
